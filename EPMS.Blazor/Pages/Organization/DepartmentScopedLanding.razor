@page "/department-scoped"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/teams/department-scoped", replace: true);
    }
}
